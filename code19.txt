from itertools import combinations_with_replacement
s, k = input().split()
k = int(k)
sorted_s = sorted(s)
combos = combinations_with_replacement(sorted_s, k)
for c in combos:
    print("".join(c))