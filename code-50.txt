import collections

if __name__ == '__main__':
    s = sorted(input().strip())
    result = collections.Counter(s).most_common(3)
    
    for char, count in result:
        print(f"{char} {count}")
